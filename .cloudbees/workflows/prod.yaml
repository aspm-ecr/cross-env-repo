apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:

jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: aspm-test
          artifact-id: 9a98da38-0115-494c-9bce-c65ee896639f
          target-environment: aspm-qa-test
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: jenkins
          artifact-id: 9a98da38-0115-494c-9bce-c65ee896639f
          target-environment: ecr-qa